export interface IState {
  state_id: string;
  zone_id: string;
  zone_name: string;
  shipping_rate: string;
  created_at: string;
  updated_at: string;
  state_name: string;
  is_delivery_paused: boolean;
}
